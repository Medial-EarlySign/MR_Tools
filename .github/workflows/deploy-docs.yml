# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Generate and Deploy Sphinx API Documentation

on:
  push:
    branches: [ "main" ]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python 3.12
      uses: actions/setup-python@v3
      with:
        python-version: "3.12"
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install sphinx numpy pandas IPython plotly sqlalchemy
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
    - name: Build docs
      run: |
        export ETL_DIR=$(realpath RepoLoadUtils/common)
        cd RepoLoadUtils/common/ETL_Infra/docs
        wget https://github.com/Medial-EarlySign/MR_LIBS/releases/download/V1.0/MES_V1.0.tar.bz2
        tar -xf MES_V1.0.tar.bz2
        export MED_PYTHON=$(realpath MedPython)
        export PYTHONPATH=${ETL_DIR}:${MED_PYTHON}
        make html
    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: RepoLoadUtils/common/ETL_Infra/docs/build/html
        publish_branch: doc-pages
